#.subsection Skill Gem

Show
    Class == "Skill Gems"
    #.choose 2 1
    GemLevel == 21
    Sockets == 5
    #.import < < styles > gem -> general++

Show
    Class == "Skill Gems"
    #.choose 2 1
    GemLevel >= 19
    Sockets == 4
    #.import < < styles > gem -> general+

Show
    Class == "Skill Gems"
    Quality == 20
    #.import < < styles > gem -> general+

Show
    Class == "Skill Gems"
    GemLevel < 19
    #.import < < styles > gem -> general