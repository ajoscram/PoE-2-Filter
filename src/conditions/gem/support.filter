#.subsection Cut & Uncut Support Gem

Show
    Class == "Support Gems"
    #.import < < styles > gem -> general

Show #.strict BASE .if Show
    BaseType == "Uncut Support Gem"
    #.import < < styles > gem -> general

Hide
    BaseType == "Uncut Support Gem"
    #.import < < styles > general -> hidden
