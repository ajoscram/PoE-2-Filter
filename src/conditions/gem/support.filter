#.subsection Cut & Uncut Support Gem

Show
    Class == "Support Gems"
    #.import STYLE | gem -> t3

Show #.strict BASE
    BaseType == "Uncut Support Gem"
    #.import STYLE | gem -> t4
    #.if Hide .import STYLE | miscellaneous -> hidden