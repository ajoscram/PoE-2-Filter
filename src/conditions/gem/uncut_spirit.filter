#.subsection Uncut Spirit Gem

# Logic based on https://poe2db.tw/us/Spirit_Gems#DropLevel

Show
    BaseType == "Uncut Spirit Gem"
    ItemLevel <= 14
    AreaLevel <= 61

Show
    BaseType == "Uncut Spirit Gem"
    ItemLevel == 15
    AreaLevel <= 65

Show
    BaseType == "Uncut Spirit Gem"
    ItemLevel == 16
    AreaLevel <= 69

Show
    BaseType == "Uncut Spirit Gem"
    ItemLevel == 17
    AreaLevel <= 73

Show
    BaseType == "Uncut Spirit Gem"
    ItemLevel == 18
    AreaLevel <= 77

Show
    BaseType == "Uncut Spirit Gem"
    ItemLevel == 19
    #.import STYLE | gem -> t2

Show
    BaseType == "Uncut Spirit Gem"
    ItemLevel == 20
    #.import STYLE | gem -> t1

Hide
    BaseType == "Uncut Spirit Gem"
    #.import STYLE | gem -> t4
    #.import STYLE | miscellaneous -> hidden