Show
    BaseType == "Withered Wand"
    #.import STYLE | mapping -> t1

Show
    BaseType == "Woven Focus"
    #.import STYLE | mapping -> t2

Show
    BaseType == "Suede Bracers"
    #.import STYLE | mapping -> t3

Show
    BaseType == "Twig Focus"
    #.import STYLE | mapping -> t4

Show
    BaseType == "Attuned Wand"
    #.import STYLE | mapping -> t5

Show

#.import STYLE | constants