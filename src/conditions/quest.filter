#.section Quest

Show
	BaseType == "Gold Key" "Silver Key" "Bronze Key"
	#.import < styles > quest -> general

Show
	Class == "Quest Items" "Atlas Upgrade Items"
	#.import < styles > quest -> important