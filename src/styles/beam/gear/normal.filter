Show #.name t1
	#.import < shared > normal -> t1
	MinimapIcon ICON_MEDIUM White UpsideDownHouse

Show #.name t2
	#.import < shared > normal -> t2
	MinimapIcon ICON_SMALL White UpsideDownHouse

Show #.name t3
	#.import < shared > normal -> t3

Show #.name t4
	#.import < shared > normal -> t4

Show #.name t5
	#.import < shared > normal -> t5